<section class="intro text-center section-padding" id="introduction">
  <!-- <div class="container">
    <div class="row">
      <div class="col-md-8 col-md-offset-2 wp1">
        {{ page.content }}
      </div>
    </div> -->
    <!-- <section class="portfolio text-center section-padding" id="portfolio"> -->
      <div class="container">
        <div class="row">
          <!-- {{ page.content }} -->
          <h1> {{ page.title }} </h1>
          <!-- <div class="img col-md-4 wp4"> -->
            <!-- <img src="{{ page.media[item.img].url }}" alt="Portfolio Item"> -->
            <!-- <img class="img-portfolio img-responsive" src="http://placehold.it/150x150"> -->
          <!-- </div> -->
          <div class="portfolioSlider">
            <ul class="slides">
              {% for row in page.header.portfolio|batch(1) %}
              <li>
                {% for item in row %}
                <div class="col-md-12 {% if item.delay %}delay-{{ item.delay }}s{% endif %}">
                  <div class="overlay-effect effects clearfix">
                    <div class="img">
                      <!-- <img src="{{ page.media[item.img].url }}" alt="Portfolio Item"> -->
                      <img class="img-portfolio img-responsive" src="http://placehold.it/150x150">

                      <!-- <div class="overlay">
                        <a href="{{ item.img_url }}" class="expand"><i class="fa fa-search"></i><br>{{ item.img_text }}</a>
                        <a class="close-overlay hidden">x</a>
                      </div> -->
                    </div>
                  </div>
                  <h2>{{ item.title }}</h2>
                  <p>{{ item.content }}</p>
                </div>
                {% endfor %}
              </li>
              {% endfor %}
            </ul>
          </div>
        </div>
      </div>
    <!-- </section> -->
  </div>
</section>
